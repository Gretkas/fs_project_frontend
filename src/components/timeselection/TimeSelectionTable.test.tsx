import { render, screen } from "@testing-library/react";

describe("Test", () => {
  test("test1", () => {
    // assert(true);
  });
});

describe("convert bit-matrix to TimeSelectionTable", () => {
  test("test1", () => {
    //assert(true);
  });
});
